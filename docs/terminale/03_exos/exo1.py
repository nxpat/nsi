# 1. Quelle erreur Python va-t-il lever lors de l'appel division(10, "2") ?
# Indiquer le nom exact de l'exception (ex: "ValueError", "TypeError", "ZeroDivisionError", ...)
nom_erreur = "..."

# 2. Compléter la précondition pour vérifier que b est un nombre (int ou float) ET qu'il est non nul
# def division(a, b):
    # À compléter
    assert ...
    return a / b


x = division(10, "2")
y = division(10, 0)
