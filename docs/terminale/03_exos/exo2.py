# 1. Proposer trois cas de tests sous forme d'une liste de triplets :
# (descriptif, (prix, pourcentage), resultat_attendu)
cas_de_tests = [
    ("Prix classique avec 20% de remise", (100, 20), 80.0),
    ("...", (..., ...), ...),
    ("...", (..., ...), ...),
]

# 2. Pourquoi le test pourcentage = 100 est-il important ?
# Indiquer "valeur limite" ou "cas général"
nature_du_cas = "..."
